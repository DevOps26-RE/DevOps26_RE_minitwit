FROM python:3.12-slim

WORKDIR /app

# Copy requirements first to leverage Docker cache
COPY test/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

# Copy the test scripts safely using relative paths
COPY test/ ./test/